import 'package:flutter/material.dart';

class PlacedOrder extends StatelessWidget {
  const PlacedOrder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text("Placed Order Page"),
      ),
    );
  }
}
