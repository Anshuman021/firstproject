import 'package:flutter/material.dart';

class RetailerHelp extends StatelessWidget {
  const RetailerHelp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text("Help Page"),
      ),
    );
  }
}
